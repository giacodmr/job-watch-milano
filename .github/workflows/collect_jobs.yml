name: Probe SuccessFactors pagination controls

on:
  push:
    branches:
      - ats-successfactors-v15
    paths:
      - sf_pagination_probe.py
      - .github/workflows/collect_jobs.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependency
        run: python -m pip install --disable-pip-version-check requests

      - name: Syntax check
        run: python -m py_compile collector.py sf_pagination_probe.py

      - name: Probe pagination controls
        run: python sf_pagination_probe.py
