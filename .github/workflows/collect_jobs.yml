name: Probe SuccessFactors inventories

on:
  push:
    branches:
      - ats-successfactors-v15
    paths:
      - sf_probe.py
      - .github/workflows/collect_jobs.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependency
        run: python -m pip install --disable-pip-version-check requests

      - name: Syntax check
        run: python -m py_compile collector.py sf_probe.py

      - name: Probe FULL SuccessFactors portals
        run: python sf_probe.py
