name: Probe SuccessFactors public HTML

on:
  push:
    branches:
      - successfactors-v15
    paths:
      - sf_probe.py
      - .github/workflows/probe_sf.yml
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --disable-pip-version-check requests
      - run: python sf_probe.py
